# Overlay

A floating modal opened via `renderer.overlay.open(modal)`, layered over a streaming background. Demonstrates the absolute-positioned paint order and the stream repair pattern on close.

Run with `bun demo/overlay.ts`.

::: code-group
<<< @/../demo/overlay.ts [demo/overlay.ts]
:::
