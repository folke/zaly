# Theme preview

Side-by-side swatches of every theme slot, rendered once per built-in theme. Useful reference when authoring a new theme — you can see every slot exercised against the same content.

Run with `bun demo/theme.ts`.

::: code-group
<<< @/../demo/theme.ts [demo/theme.ts]
:::
