# Code

Standalone `code()` widgets with titles across a few languages. Exercises the shiki highlighter and the `code` / `codeTitle` theme slots.

Run with `bun demo/code.ts`.

::: code-group
<<< @/../demo/code.ts [demo/code.ts]
:::
