# Diff

Renders a set of line-range edits against an original file as a unified diff — the shape tools like `Edit` emit once `old_string`/`new_string` have been resolved to indices.

Run with `bun demo/diff.ts`.

::: code-group
<<< @/../demo/diff.ts [demo/diff.ts]
:::
