# Custom widget

A full agent-harness UI composed from `box`, `text`, and a custom `widget(...)` status line that swaps icon + color based on its reactive state. Rendered through `loadTheme()` for a couple of built-in themes.

Run with `bun demo/widget.ts`.

::: code-group
<<< @/../demo/widget.ts [demo/widget.ts]
:::
