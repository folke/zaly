# Input + paste

Minimal echo chat. Typed messages are appended to the stream; `ctrl-v` pastes text inline, or attaches pasted images/files via the `attach` event.

Run with `bun demo/input.ts`.

::: code-group
<<< @/../demo/input.ts [demo/input.ts]
:::
