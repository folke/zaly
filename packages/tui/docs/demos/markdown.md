# Markdown

Renders `demo/demo.md` through the `markdown` widget — headings, lists, tables, fenced code with syntax highlighting, inline images. A good smoke test for the full markdown pipeline.

Run with `bun demo/md.ts`.

::: code-group
<<< @/../demo/md.ts [demo/md.ts]
:::
