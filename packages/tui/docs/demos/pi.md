# Pi — kitchen-sink showcase

The full-fat demo: input, autocomplete (slash + files), streaming markdown, progress, spinner, help overlay, theme swatches. Useful as a reference for wiring every major widget together in one app.

Run with `bun demo/pi.ts`.

::: code-group
<<< @/../demo/pi.ts [demo/pi.ts]
:::
